// Client-side exports only
export { createClient, supabase } from './client';

// Note: For server-side imports, use:
// import { createClient } from '@/lib/supabase/server';
// import { getServiceClient } from '@/lib/supabase/server';